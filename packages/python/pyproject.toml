[project]
name = "fidel-tools"
version = "0.1.0"
description = "Python port of Fidel Tools - Amharic language preprocessing toolkit"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
authors = [
    { name = "Fidel Tools Authors" }
]
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
